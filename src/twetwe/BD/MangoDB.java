package twetwe.BD;

public class MangoDB {
//Message(user_id ,date,user_name,content)
	/**
	 * user_id : 158
	 * date : "xxx"
	 * content ; "blabla
	 * user_name : "toto*
	 * 
	 * id: object(ID(AFFBL) 
	 * 
	 * db.message.find({user_id : 3 }) 
	 * retourn les documents ou le champ user_id est 3
	 * 
	 *  
	 *  
	 *  MangoClien.mango= MongoClients.create(new MangoClien URL("mangodb://localhost:27017"));
	 *  MangoDataBase db =mango.getDataBase c " 3I017
	 *  
	 *  mangoCollection<Document> message_collection = db.getCollection("message");
	 *  
	 *  Document querry = new Document();
	 *  
	 *  query.append("user_id" : 
	 *  Date = date_avant = calendar.getTime();
	 *  query.append("date",new Document("$gt" ,date_avant))
	 *  MongoCursor<Document> cursor_message_colletion.find(query) iterator
	 *  
	 *  
	 *  
	 *  
	 *  
	 *  
	 *  
	 *  4.4
	 *  MangoShell
	 *  db.cars.find({"make " :"FORD"; "model" : { $in :["Galaxy","Mustang" "Meteor"]}}
	 *  
	 *  Mango JavaDriver 
	 *  Document query = new Document()
	 *  query.append("make","Ford");
	 *  String model[]=new String'[]{ "Galaxy","Mustang","Mistor"}
	 *  Document in = new Document("$in,models)
	 *  query.append("model",in);
	 *  ma_collection.find(query)
	 *  */
	
	
	
	
}
